# Database Configuration
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/shop_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}

# Server Configuration
server.port=8080

# Shop Configuration
shop.name=Manisha Electronics
shop.address=123 Shop Street, City
shop.phone=9876543210
shop.gstin=22AAAAA0000A1Z5