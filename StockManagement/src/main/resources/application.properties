# Database Configuration
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/shop_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}

# Server Configuration
server.port=${PORT:8080}

# Shop Configuration
shop.name=MANISHA ELECTRONIC
shop.address=EDEN GROVE Building, Nr. State Bank of India, Opp. Govt. Higher Secondary, Thane Road, Valpoi, Satartia - Goa
shop.phone=9309736172, 70205592347
shop.gstin=30AMYPN1753F1ZY

# Security & Authentication Configuration
auth.jwt.secret=${JWT_SECRET:ManishaElectronicsSecretSigningKey2026SecureRetailKeyMustBeLongEnough}
auth.jwt.expiration-days=30
auth.admin.username=${ADMIN_USERNAME:admin}
auth.admin.password=${ADMIN_PASSWORD:admin123}
auth.admin.pin=${ADMIN_PIN:1234}